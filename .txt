  <div className="flex items-start justify-between mb-4">
        <div
          className={`w-16 h-16 rounded-lg flex items-center justify-center text-3xl overflow-hidden 
    ${job.logo && job.logo.startsWith("data:image") ? "bg-transparent" : job.bgColor}
  `}
        >
          {job.logo && job.logo.startsWith("data:image") ? (
            <img
              src={job.logo}
              alt="Company Logo"
              className="w-full h-full object-contain"
            />
          ) : (
            <span>{job.logo}</span>
          )}
        </div>

      </div>